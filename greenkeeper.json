{
  "commitMessages": {
    "dependencyUpdate": "chore(ex): update ${dependency} to version ${version}",
    "devDependencyUpdate": "chore(ex): update ${dependency} to version ${version}"
  },
  "groups": {
    "examples": {
      "packages": [
        "examples/expo/package.json"
      ],
      "ignore": ["expo", "react-native", "react"]
    },
    "default": {
      "packages": [
        "package.json"
      ]
    }
  }
}
